export {};
// import { Test, TestingModule } from '@nestjs/testing';
// import { ModuleMetadata } from '@nestjs/common/interfaces/modules/module-metadata.interface';
// import { NestCoreModule, ValidatorModule } from '@nestpkg/core';
//
// export async function setupContext(metadata: ModuleMetadata) {
//   metadata = {
//     ...metadata,
//     imports: [NestCoreModule, ValidatorModule, ...(metadata.imports || [])],
//   };
//
//   const moduleRef: TestingModule = await Test.createTestingModule(
//     metadata
//   ).compile();
//
//   await moduleRef.init();
//
//   return moduleRef;
// }
